<!DOCTYPE html>
<html lang="pt-BR">
  <!--Comando EJS de incluir o head -->
  <%- include("../partials/head"); %>
  <body class="container pt-5">
    <!--Comando EJS de incluir o header -->
    <%- include("../partials/header", {pagina: "Home"}); %>
    <main>
      <div class="bg-dark text-white text-center p-5">
        <h1>Node com EJS</h1>
        <p><%= subtitle %></p>
        <p>Vamos aprender a trabalhar com arquivos EJS</p>

      </div>
      <div>
        <ul>
          <% qualidades.forEach(function(qualidade){ %>
            <li>
              <strong><%= qualidade.title %></strong><%= qualidade.mensagem%>
            </li>
          <% }) %>
        </ul>
      </div>
    </main>
    <!--Comando EJS de incluir o footer -->
    <%- include("../partials/footer"); %>
  </body>
</html>